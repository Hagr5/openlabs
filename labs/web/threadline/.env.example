# Local development flag — copy this file to .env before running
# `docker compose up --build`. The platform injects the real flag at
# deploy time via its own secret manager.
FLAG=duck{REPLACE_ME_BEFORE_RUNNING}
