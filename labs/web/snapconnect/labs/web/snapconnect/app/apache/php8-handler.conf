# Author note (spoiler): this is the deployment misconfiguration under test.
# During an engine upgrade drill, ops mapped .php8 onto the PHP handler and
# never removed it. Files anywhere under the document root, including
# /uploads, are executed as PHP when their name ends in .php8.
<FilesMatch \.php8$>
    SetHandler application/x-httpd-php
</FilesMatch>
