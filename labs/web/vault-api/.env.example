# vault-api lab — environment template
# Copy this file to .env and replace FLAG with a unique duck flag:
#     cp .env.example .env
# The flag format is duck{...}. The flag_hash in lab.yml must be the
# SHA-256 of the exact flag value, braces included.
FLAG=duck{replace_me_with_your_challenge_flag}