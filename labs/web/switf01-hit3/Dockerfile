FROM scratch
# This lab runs as a multi-service stack via docker-compose.yml.
# Individual service Dockerfiles are in app/backend/ and app/frontend/.
#
# To start the lab:
#   docker compose up --build -d
